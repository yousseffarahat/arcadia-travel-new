import { AnimationOnScrollDirective } from './animation-on-scroll.directive';

describe('AnimationOnScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimationOnScrollDirective();
    expect(directive).toBeTruthy();
  });
});
